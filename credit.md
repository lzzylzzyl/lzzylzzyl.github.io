2023年10月1日
项目启动

决定开发一个知识共享平台，命名为 Wiki 主页。

确定核心功能：主页展示、分类浏览、关于我们、帮助页面。

设计页面布局和整体风格，选择深色背景和动态粒子效果。

2023年10月2日
主页开发

完成主页的基本结构，包括头部、导航栏、内容区域和页脚。

引入 particles.js 实现背景粒子效果。

添加鼠标点击特效，增强用户交互体验。

调整粒子数量和运动速度，优化视觉效果。

2023年10月3日
样式优化

将 CSS 样式提取到单独的 styles.css 文件中，方便复用。

优化页面布局，确保内容在粒子效果上方显示。

调整字体大小、颜色和间距，提升可读性。

2023年10月4日
脚本优化

将 JavaScript 代码提取到单独的 scripts.js 文件中。

修复点击特效的内存泄漏问题，确保动画结束后移除元素。

优化粒子系统的配置，减少性能消耗。

2023年10月5日
分类页面开发

完成分类页面的基本结构。

添加热门分类和推荐阅读模块。

复用主页的样式和脚本，确保风格一致。

2023年10月6日
关于我们页面开发

完成关于我们页面的基本结构。

添加团队介绍、使命和联系方式模块。

优化内容排版，提升用户体验。

2023年10月7日
帮助页面开发

完成帮助页面的基本结构。

添加常见问题和联系方式模块。

优化页面导航，确保用户能快速找到所需信息。

2023年10月8日
测试与优化

在所有主流浏览器（Chrome、Firefox、Edge）中测试页面兼容性。

修复粒子效果在移动设备上的显示问题。

优化页面加载速度，减少资源文件大小。

2023年10月9日
项目总结

完成所有页面的开发和测试。

撰写开发日志，记录项目进展和遇到的问题。

准备项目上线，部署到服务器。

功能优化
图像交互升级

支持 Plotly 图像缩放、平移和下载。

默认坐标轴范围调整为动态适配数据。

代码重构

分离 HTML/CSS/JavaScript 代码，增强可维护性。

封装绘图逻辑到独立函数 plotFunctions()。

UI 改进

输入框添加实时语法高亮（通过 oninput 事件）。

错误提示样式优化（红色边框 + Toast 通知）。

2024-05-31
任务：搭建基础框架
进展：

完成前端 HTML 页面，包括表单和评论列表。

实现 JavaScript 提交逻辑，使用 XMLHttpRequest 发送 POST 请求。

搭建 Python 服务器，支持 CORS 和 OPTIONS 预检请求。

实现 POST 请求处理，解析 XML 数据并保存到 comments.xml 文件。

问题：

前端提交后返回 501 错误（Unsupported method 'POST'）。

XML 解析失败，服务器无法正确处理数据。

解决方案：

检查服务器代码，确认 do_POST 方法已实现。

添加调试日志，打印接收到的原始数据。

修正 XML 解析逻辑，确保根节点匹配。

2024-06-01
任务：修复 XML 解析与存储问题
进展：

修正前端生成的 XML 结构，确保与服务器期望格式一致。

修改服务器逻辑，支持动态加载和保存 XML 文件。

添加时间戳字段，记录评论提交时间。

问题：

服务器返回的 JSON 响应格式不正确，导致前端无法解析。

中文内容保存到 XML 文件后出现乱码。

解决方案：

使用 Python 的 json 模块生成标准 JSON 响应。

在 XML 文件写入时指定 encoding="utf-8"。

2024-06-02
任务：优化错误处理与用户体验
进展：

添加前端表单验证，限制姓名和评论内容的长度。

实现服务器端输入验证，确保数据完整性。

在前端显示操作反馈（成功/失败消息）。

问题：

服务器在高并发情况下性能较差。

XML 文件读写效率低，不适合大规模数据存储。

解决方案：

考虑使用数据库（如 SQLite）替代 XML 文件存储。

优化服务器代码，减少文件读写次数。

2024-06-03
任务：性能优化与代码重构
进展：

重构服务器代码，使用 Flask 框架替代 http.server。

实现 SQLite 数据库存储，提升数据读写效率。

添加分页功能，限制每次加载的评论数量。

问题：

Flask 部署后出现跨域问题。

数据库初始化脚本未正确处理已有数据迁移。

解决方案：

使用 flask-cors 扩展解决跨域问题。

编写数据迁移脚本，将现有 XML 数据导入数据库。

2024-06-04
任务：测试与部署
进展：

完成单元测试，覆盖所有核心功能。

使用 Docker 容器化部署，确保环境一致性。

配置 Nginx 反向代理，提升服务器性能。

问题：

Docker 容器内时间戳与宿主机不一致。

Nginx 配置错误导致静态文件无法加载。

解决方案：

在 Dockerfile 中设置时区环境变量。

修正 Nginx 配置文件，确保静态资源路径正确。